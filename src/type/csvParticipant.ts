export type csvParticipant = {
   is_disabled?: boolean,
   original_id: number,
   participant_source_id: number,
   first_name: string,
   last_name: string,
   birth_date: string,
   phone_number: string,
   email_address?: string,
   municipality_epci_id: number,
   rowData: string,
}
